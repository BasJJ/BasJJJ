<UserControl x:Class="CoursesManager.UI.Views.StudentManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CoursesManager.UI.Views"
             xmlns:rs="clr-namespace:CoursesManager.UI.Resources"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DesignHeight="1030" d:DesignWidth="1920" d:Background="{StaticResource SecondaryGradiant}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="50" />
            <RowDefinition Height="3*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="1" CornerRadius="15">
                <rs:PlaceholderTextBox Placeholder="Hinted search text" VerticalContentAlignment="Center" FontSize="36" BorderBrush="{x:Null}" ></rs:PlaceholderTextBox>
            </Border>

            <!--<rs:SearchBox Grid.Column="1" Text="{Binding SearchText, Mode=TwoWay}"
                             SearchCommand="{Binding SearchCommand}" />-->

            <Button Grid.Column="3" Style="{StaticResource PrimaryButton}">Data importeren</Button>
            <Button Grid.Column="5" Style="{StaticResource PrimaryButton}">Data exporteren</Button>
        </Grid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="50" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="1">
            </DataGrid>
        </Grid>
    </Grid>
</UserControl>